---
bootstrap_cloud_provider: "{{ cloud_provider | default('ec2') }}"
bootstrap_agnosticg_repo: https://github.com/treddy08/agnosticg.git
bootstrap_vault_namespace: vault
bootstrap_external_secrets_version: 0.9.2
bootstrap_backstage_namespace: backstage
bootstrap_quay_admin_user: quayadmin
bootstrap_quay_admin_password: "{{ common_password }}"
bootstrap_common_password: "{{ common_password }}"
bootstrap_cosign_password: redhat
bootstrap_openshift_pipelines_namespace: openshift-pipelines


bootstrap_rhacs_namespace: stackrox
bootstrap_rhacs_admin_password: "{{ common_password }}"

bootstrap_tpa_namespace: trusted-profile-analyzer
bootstrap_tpa_oidc_client_id: walker #notsecret
bootstrap_tpa_oidc_client_secret: 5460cc91-4e20-4edd-881c-b15b169f8a79 #notsecret
